import { Card, useTheme } from "@mui/material";

export const HomeStack = () => {
	const theme = useTheme();
	return (
		<Card
			sx={{
				flexGrow: 1,
				height: "100%",
			}}
		>
			2
		</Card>
	);
};
